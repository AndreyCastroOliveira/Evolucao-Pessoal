# Importando o módulo random para gerar números aleatórios

import random

num = random.randint(10, 20)#randint gera um número inteiro aleatório entre os dois números passados como parâmetro
print(num)

